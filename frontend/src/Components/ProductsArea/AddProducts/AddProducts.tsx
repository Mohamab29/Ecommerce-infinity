import { Typography } from "@material-ui/core";
import "./AddProducts.css";

function AddProducts(): JSX.Element {
  return (
    <div className="AddProducts">
      <Typography variant="h1">Adding products</Typography>
    </div>
  );
}

export default AddProducts;
